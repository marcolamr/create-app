---
title: Variáveis de ambiente
description: Env comuns nos projetos gerados.
---

Validação em `src/env.js` com Zod.

- `DATABASE_URL` — com Drizzle  
- `BETTER_AUTH_*` — com auth + banco  
- `NEXT_PUBLIC_*` — URLs do cliente  

Use `.env.example` como referência versionada.
